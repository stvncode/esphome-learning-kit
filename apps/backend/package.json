{
  "name": "@esphome-learning-kit/backend",
  "type": "package",
  "private": true,
  "module": "src/index.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun"
  },
  "dependencies": {
    "@esphome-learning-kit/types": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
